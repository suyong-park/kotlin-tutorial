/*
* ? 키워드는 null 값을 허용한다.
* !! 키워드는 null 값을 허용하지 않는다. 다만, null 값이 들어오게 되는 경우 예외를 발생시키므로 null 안전성에 좋지 않다. 권장되지 않는다.
* ?: 키워드는 null 값이 들어가 있다면 우측의 기본값을 반환하게 된다.
* ?. 키워드는 null 값이 들어오게 되는 경우 null 값을 그대로 반환한다.
*/